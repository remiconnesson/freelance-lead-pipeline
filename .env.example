# Postgres connection string. Neon recommended.
# On Vercel, adding the Neon integration sets this automatically.
DATABASE_URL=

# Only needed when running outside Vercel. Vercel deployments
# authenticate to the AI Gateway via OIDC, so leave this unset there.
# AI_GATEWAY_API_KEY=
